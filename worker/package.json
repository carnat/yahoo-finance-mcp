{
  "name": "yahoo-finance-mcp-worker",
  "version": "2.5.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "test:transcript-contract": "node --no-warnings scripts/test-transcript-contract.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260924.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.138.0"
  }
}
